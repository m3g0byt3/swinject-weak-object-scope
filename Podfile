# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwinjectObjectScopeTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwinjectObjectScopeTest
  pod 'Swinject'

  target 'SwinjectObjectScopeTestTests' do
    inherit! :search_paths
   
    # Pods for testing
    pod 'Swinject'
  
  end

end
